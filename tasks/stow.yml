- name: Clone dotfiles
  ansible.builtin.git:
    repo: "https://github.com/pypeaday/dotfiles.git"
    dest: "{{ lookup('env', 'HOME') }}/dotfiles"
    update: no
  ignore_errors: yes
  tags:
    - install
    - terminal
    - core
    - stow
    - dotfiles
